1，目前请求都是直接发送到数据库端的，但实际生产环境中，需要通过连接器，请求主服务获取，已经准备好了函数和接口。
2，git仓库目前有问题，打个包先保存。
3，目前的路径机制都是使用绝对严格的绝对路径，同时是用索引来定位的。
也就意味着实际工作地址，不能被改变，一旦改变，程序无法准确完成工作。
使用虚拟机用户不能改变文件夹。





1.文件体系不变，加入镜像功能
2.备份功能，还要再继续，没有写完，不单单是备份压缩包，每个文件都要备份，加入文加名字记下来
加入错误处理端。后台处理。以后再说。不急，先完成领导说的镜像功能，细节以后再完善。





改写方式1，redis 报的，然后去这里面拿，不断循环redis 去里面找地址，然后下载，也就是数据库，不用动，每次自动更新
先用mysql方式，不行在用redis,就是



12.31
现在的问题是部署问题，前台启动一切都好
如或是后天就会有各种问题，线程起不来，改成顺序执行的话，win命令执行部了



两个人抱一个，名字不能重复，这个以后再说。



Reids会不会更新掉



一对多，没啥问题







